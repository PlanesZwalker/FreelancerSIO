{% extends 'base.html.twig' %}
{% block titre %}

        <h1 id="logo"><a class="bigtitle">Offres</a></h1>  

{% endblock %}

{% block body %}    
    <div class="formulairetop">
        <h2 class="mysubtitle">Afficher une Offre</h2>
        <div class="mycontentformshow">
            <ul>
                <li class="returnlist">
                    <a class="btn btn-default" href="{{ path('offre_index') }}">Retourner à la liste</a>
                </li>
            </ul>
            <table  class="tableautitle">
                <tbody>
                    <tr>
                        <th class="mytdtabshow">id_offre</th>
                        <td class="mytdtabshow">{{ offre.id_offre }}</td>
                    </tr>
                    <tr>
                        <th class="mytdtabshow2">Tarif</th>
                        <td class="mytdtabshow2">{{ offre.tarif }}</td>
                    </tr>
                    <tr>
                        <th class="mytdtabshow">Delai</th>
                        <td class="mytdtabshow">{{ offre.delai }}</td>
                    </tr>
                    <tr>
                        <th class="mytdtabshow2">Propcommerciale</th>
                        <td class="mytdtabshow2">{{ offre.propCommerciale }}</td>
                    </tr>
                    <tr>
                        <th class="mytdtabshow">Particularite</th>
                        <td class="mytdtabshow">{{ offre.particularite }}</td>
                    </tr>
                    <tr>
                        <th class="mytdtabshow2">id_freelancer</th>
                        <td class="mytdtabshow2">{{ offre.id_freelancer }}</td>
                    </tr>
                </tbody>
            </table>

            <ul>
                <li class="myeditli">
                    <a class="btn btn-info" style="color:white;" href="{{ path('offre_edit', { 'id': offre.id_offre }) }}">Modifier</a>
                </li>
                <li class="mydeleteli">
                    {{ form_start(delete_form) }}
                        <input type="submit" value="Delete">
                    {{ form_end(delete_form) }}
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
