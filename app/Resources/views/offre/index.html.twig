{% extends 'base.html.twig' %}
{% block titre %}

        <h1 id="logo"><a class="bigtitle">Offres</a></h1>  

{% endblock %}

{% block body %}    
    <div class="formulairetop">
        <h1 class="mysubtitle">Liste des Offres</h1>
        <div class="mycontentform">
            <table>
                <thead>
                    <tr>
                        <th>Idoffre</th>
                        <th>Tarif</th>
                        <th>Delai</th>
                        <th>Propcommerciale</th>
                        <th>Particularite</th>
                        <th>Idfreelancer</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for offre in offres %}
                    <tr>
                        <td><a href="{{ path('offre_show', { 'id': offre.idOffre }) }}">{{ offre.idOffre }}</a></td>
                        <td>{{ offre.tarif }}</td>
                        <td>{{ offre.delai }}</td>
                        <td>{{ offre.propCommerciale }}</td>
                        <td>{{ offre.particularite }}</td>
                        <td>{{ offre.idFreelancer }}</td>
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('offre_show', { 'id': offre.idOffre }) }}">Afficher</a>
                                </li>
                                <li>
                                    <a href="{{ path('offre_edit', { 'id': offre.idOffre }) }}">Modifier</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

            <ul>
                <li>
                    <a href="{{ path('offre_new') }}">Créer un nouveau  offre</a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
