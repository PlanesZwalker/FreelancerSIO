{% extends 'base.html.twig' %}
{% block titre %}

        <h1 id="logo"><a class="bigtitle">Freelancer</a></h1>  

{% endblock %}

{% block body %}    
    <div class="formulairetop">
        <h2 class="mysubtitle">Afficher le Profil d'un Freelancer</h2>
        <div class="mycontentform">
            <table>
                <tbody>
                    <tr>
                        <th>id_freelancer</th>
                        <td>{{ freelancer.id_freelancer }}</td>
                    </tr>
                    <tr>
                        <th>Urlcv</th>
                        <td>{{ freelancer.urlCv }}</td>
                    </tr>
                    <tr>
                        <th>Urlphoto</th>
                        <td>{{ freelancer.urlPhoto }}</td>
                    </tr>
                    <tr>
                        <th>Age</th>
                        <td>{{ freelancer.age }}</td>
                    </tr>
                    <tr>
                        <th>Iduser</th>
                        <td>{{ freelancer.idUser }}</td>
                    </tr>
                </tbody>
            </table>

            <ul>
                <li>
                    <a href="{{ path('freelancer_index') }}">Retourner à la liste</a>
                </li>
                <li>
                    <a href="{{ path('freelancer_edit', { 'id': freelancer.id_freelancer }) }}">Modifier</a>
                </li>
                <li>
                    {{ form_start(delete_form) }}
                        <input type="submit" value="Delete">
                    {{ form_end(delete_form) }}
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
