{% extends 'base.html.twig' %}
{% block titre %}

        <h1 id="logo"><a class="bigtitle">Freelancer</a></h1>  

{% endblock %}

{% block body %}    
    <div class="formulairetop">
        <h1 class="mysubtitle">Liste des Freelancers</h1>
        <div class="mycontentform">
            <table>
                <thead>
                    <tr>
                        <th>Idfreelancer</th>
                        <th>Urlcv</th>
                        <th>Urlphoto</th>
                        <th>Age</th>
                        <th>Iduser</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for freelancer in freelancers %}
                    <tr>
                        <td><a href="{{ path('freelancer_show', { 'id': freelancer.idFreelancer }) }}">{{ freelancer.idFreelancer }}</a></td>
                        <td>{{ freelancer.urlCv }}</td>
                        <td>{{ freelancer.urlPhoto }}</td>
                        <td>{{ freelancer.age }}</td>
                        <td>{{ freelancer.idUser }}</td>
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('freelancer_show', { 'id': freelancer.idFreelancer }) }}">Afficher</a>
                                </li>
                                <li>
                                    <a href="{{ path('freelancer_edit', { 'id': freelancer.idFreelancer }) }}">Modifier</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

            <ul>
                <li>
                    <a href="{{ path('freelancer_new') }}">Créer un nouveau  freelancer</a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
