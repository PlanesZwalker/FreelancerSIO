{% extends 'base.html.twig' %}
{% block titre %}

        <h1 id="logo"><a class="bigtitle">Paiements</a></h1>  

{% endblock %}

{% block body %}    
    <div class="formulairetop">
        <h2 class="mysubtitle">Liste des Paiements</h2>
        <div class="mycontentform">
            <table>
                <thead>
                    <tr>
                        <th>id_paiement</th>
                        <th>Etat</th>
                        <th>Dpsociete</th>
                        <th>Dpfreelancer</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for paiement in paiements %}
                    <tr>
                        <td><a href="{{ path('paiement_show', { 'id': paiement.id_paiement }) }}">{{ paiement.id_paiement }}</a></td>
                        <td>{{ paiement.etat }}</td>
                        <td>{{ paiement.dpSociete }}</td>
                        <td>{{ paiement.dpFreelancer }}</td>
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('paiement_show', { 'id': paiement.id_paiement }) }}">Afficher</a>
                                </li>
                                <li>
                                    <a href="{{ path('paiement_edit', { 'id': paiement.id_paiement }) }}">Modifier</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

            <ul>
                <li>
                    <a href="{{ path('paiement_new') }}">Créer un nouveau  paiement</a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
