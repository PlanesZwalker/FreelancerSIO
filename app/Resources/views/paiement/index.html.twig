{% extends 'base.html.twig' %}
{% block titre %}

        <h1 id="logo"><a class="bigtitle">Paiements</a></h1>  

{% endblock %}

{% block body %}    
    <div class="formulairetop">
        <h1 class="mysubtitle">Liste des Paiements</h1>
        <div class="mycontentform">
            <table>
                <thead>
                    <tr>
                        <th>Idpaiement</th>
                        <th>Etat</th>
                        <th>Dpsociete</th>
                        <th>Dpfreelancer</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for paiement in paiements %}
                    <tr>
                        <td><a href="{{ path('paiement_show', { 'id': paiement.idPaiement }) }}">{{ paiement.idPaiement }}</a></td>
                        <td>{{ paiement.etat }}</td>
                        <td>{{ paiement.dpSociete }}</td>
                        <td>{{ paiement.dpFreelancer }}</td>
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('paiement_show', { 'id': paiement.idPaiement }) }}">Afficher</a>
                                </li>
                                <li>
                                    <a href="{{ path('paiement_edit', { 'id': paiement.idPaiement }) }}">Modifier</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

            <ul>
                <li>
                    <a href="{{ path('paiement_new') }}">Créer un nouveau  paiement</a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
