{% extends 'base.html.twig' %}
{% block titre %}

        <h1 id="logo"><a class="bigtitle">Paiements</a></h1>  

{% endblock %}

{% block body %}    
    <div class="formulairetop">
        <h2 class="mysubtitle">Edition d'un Paiement</h2>
        <div class="mycontentform">
            <ul>
                <li class="returnlist">
                    <a data-ajax="false" class="btn btn-default" href="{{ path('paiement_index') }}">Retourner à la liste</a>
                </li>
            </ul>
            <table class="tableautitle">
                <tbody>
                    {{ form_start(edit_form) }}
                        {{ form_widget(edit_form) }}
                        <input type="submit"  class="myeditinput" value="Modifier les informations de paiement" />
                    {{ form_end(edit_form) }}
                    <ul>
                        <li  class="mydeleteli">
                            {{ form_start(delete_form) }}
                                <input type="submit"  value="Supprimer le paiement">
                            {{ form_end(delete_form) }}
                        </li>
                    </ul> 
               </tbody>
            </table>
        </div>
    </div>
{% endblock %}
