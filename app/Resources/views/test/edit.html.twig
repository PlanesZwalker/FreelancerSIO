{% extends 'base.html.twig' %}
{% block titre %}

    <h1 id="logo"><a class="bigtitle">Tests</a></h1>  

{% endblock %}

{% block body %}    
    <div class="formulairetop">
        <h2 class="mysubtitle">Editer un Test</h2>
        <div class="mycontentform">
            <ul>
                <li class="returnlist">
                    <a data-ajax="false" class="btn btn-default" href="{{ path('test_index') }}">Retourner à la liste</a>
                </li>
            </ul>
            <table  class="tableautitle">
                <tbody>
                    {{ form_start(edit_form) }}
                    {{ form_row(edit_form.description) }}
                    {{ form_row(edit_form.urlTest) }}
                    {{ form_row(edit_form.resultat) }}
                
                    {{ form_row(edit_form.id_freelancer) }}
                    {{ form_row(edit_form.id_competence) }}
                
                </tbody>
            </table>             
            <input type="submit"  class="myeditinput"  value="Modifier le test" />
            {{ form_end(edit_form) }}

          {% if is_granted("ROLE_ADMIN") %}            
            <ul>
                 <li class="mydeleteli ">
                  
                        {{ form_start(delete_form) }}
                            <input type="submit" rel="external" value="Supprimer le test" />
                        {{ form_end(delete_form) }}
                   
                </li>
            </ul> 
            {% endif %}
        </div>
    </div>
{% endblock %}
