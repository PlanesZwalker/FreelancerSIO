{% extends 'base.html.twig' %}
{% block titre %}

        <h1 id="logo"><a class="bigtitle">Tests</a></h1>  

{% endblock %}

{% block body %}    
    <div class="formulairetop">
        <h2 class="mysubtitle">Ajouter un Test</h2>
        <div class="mycontentform">
            <ul>
                <li class="returnlist">
                    <a data-ajax="false" class="btn btn-default" href="{{ path('test_index') }}">Retourner à la liste</a>
                </li>
            </ul>
            <table  class="tableautitle">
                <tbody>
                {{ form_start(form) }}
                        {{ form_row(form.description) }}
                        {{ form_row(form.url_test) }}
                        {{ form_row(form.resultat) }}
                        {{ form_row(form.date_passage) }}
                    
                    <input data-ajax="false" type="submit" value="Ajouter le Test" />
                {{ form_end(form) }}
                 </tbody>
             </table> 
            <ul>
               
            </ul>
        </div>
    </div>
{% endblock %}
