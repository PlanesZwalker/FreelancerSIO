{% extends 'base.html.twig' %}
{% block titre %}

        <h1 id="logo"><a class="bigtitle">Compétences</a></h1>  

{% endblock %}

{% block body %}    
    <div class="formulairetop">
        <h2 class="mysubtitle">Voir les Compétences</h2>
        <div class="mycontentformshow">
            <ul>
                <li class="returnlist">
                    <a class="btn btn-default" href="{{ path('competence_index') }}">Retourner à la liste</a>
                </li>
            </ul>
            <table class="tableautitle">
                <tbody>
                    <tr>
                        <th class="mytdtabshow">Competence</th>
                        <td class="mytdtabshow">{{ competence.id_competence }}</td>
                    </tr>
                    <tr>
                        <th class="mytdtabshow2">Categorie</th>
                        <td class="mytdtabshow2">{{ competence.categorie }}</td>
                    </tr>
                    <tr>
                        <th class="mytdtabshow">Description</th>
                        <td class="mytdtabshow">{{ competence.description }}</td>
                    </tr>
                    <tr>
                        <th class="mytdtabshow2">Freelancer</th>
                        <td class="mytdtabshow2">{{ competence.id_freelancer }}</td>
                    </tr>
                </tbody>
            </table>

            <ul>
                <li class="myeditli">
                    <a class="btn btn-info" style="color:white;" href="{{ path('competence_edit', { 'id': competence.id_competence }) }}">Modifier</a>
                </li>
                <li class="mydeleteli">
                    {{ form_start(delete_form) }}
                        <input type="submit" value="Delete">
                    {{ form_end(delete_form) }}
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
