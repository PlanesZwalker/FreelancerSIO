{% extends 'base.html.twig' %}
{% block titre %}

        <h1 id="logo"><a class="bigtitle">Projets</a></h1>  

{% endblock %}

{% block body %}    
    <div class="formulairetop">
        <h2 class="mysubtitle">Liste des Projets</h2>
        <div class="mycontentform">
            <ul>
                <li class="returnlist">
                    <a data-ajax="false" class="btn btn-default" href="{{ path('projet_index') }}">Retourner à la liste</a>
                </li>
            </ul>
            <table  class="tableautitle">
                <tbody>
                    {{ form_start(form) }}
                        {{ form_row(form.description) }}
                        {{ form_row(form.etape) }}
                        {{ form_row(form.description) }}
                        <input type="submit" value="Ajouter le projet" />
                    {{ form_end(form) }}
                </tbody>
             </table> 
            <ul>
             
            </ul>
        </div>
    </div>
{% endblock %}
