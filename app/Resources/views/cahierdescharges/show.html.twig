{% extends 'base.html.twig' %}
{% block titre %}

    <h1 id="logo"><a class="bigtitle">Cahiers des Charges</a></h1>  

{% endblock %}
{% block body %}
    <div class="formulairetop">
        <h2 class="mysubtitle">Cahier des charges</h2>
        <div class="mycontentformshow">
            <ul>
                <li class="returnlist">
                    <a class="btn btn-success" href="{{ path('cahierdescharges_index') }}">Retourner à la liste</a>
                </li>

            </ul>
            <table  class="tableautitle">
                <tbody>
                    <tr>
                        <th class="mytdtabshow">Projet</th>
                        <td class="mytdtabshow">{{  dump(projets) }}</td>
                    </tr>
                    <tr>
                        <th class="mytdtabshow2">Tarif</th>
                        <td class="mytdtabshow2">{{ cahierdescharges.tarif }}</td>
                    </tr>
                    <tr>
                        <th class="mytdtabshow">Delai</th>
                        <td class="mytdtabshow">{{ cahierdescharges.delai }}</td>
                    </tr>
                    <tr>
                        <th class="mytdtabshow2">Contrat</th>
                        <td class="mytdtabshow2">{{  dump(societes) }}</td>
                    </tr>
                </tbody>
            </table>

            <ul>
                <li class="myeditli">
                    <a  class="btn btn-info" style="color:white;"  href="{{ path('cahierdescharges_edit', { 'id': cahierdescharges.id }) }}">Modifier</a>
                </li>
                <li class="mydeleteli">
                    {{ form_start(delete_form) }}
                    <input  type="submit" value="Supprimer">
                    {{ form_end(delete_form) }}
                </li>
            </ul>
        </div>
    {% endblock %}
