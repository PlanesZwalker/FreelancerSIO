{% extends 'base.html.twig' %}
{% block titre %}

        <h1 id="logo"><a class="bigtitle">Messages</a></h1>  

{% endblock %}

{% block body %}    
    <div class="formulairetop">
        <h2 class="mysubtitle">Envoyer un Message à</h2>
        <div class="mycontentform">           
            <ul>
                <li class="returnlist">
                    <a class="btn btn-default" href="{{ path('message_index') }}">Retourner à la liste</a>
                </li>
            </ul>
            {{ form_start(form) }}
            
                {{ form_row(form.type) }}
                
                {{ form_row(form.sujet) }}

                {{ form_row(form.contenu) }}
 
                <input type="submit" value="Envoyer" />
                
            {{ form_end(form) }}

                   </div>
    </div>
{% endblock %}
