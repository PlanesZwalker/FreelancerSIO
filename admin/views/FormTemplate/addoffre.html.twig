{% extends "layout.html.twig" %}

{% block css %}
    <link rel="stylesheet" href="//code.jquery.com/ui/1.11.4/themes/smoothness/jquery-ui.css">
{% endblock %}

{#Calendrier pour Séléctionner la date de l'offre#}
{% block javascript %}
    <script>
        $(function() {
            $( "#datepicker" ).datepicker({
                dateFormat : 'yy/mm/dd',
                yearRange: '-100y:c+nn',
                changeMonth: true,
                changeYear: true,
                dayNamesMin: ['Lun', 'Ma', 'Me', 'Jeu', 'Ven', 'Sam', 'Dim']
            });
        });
    </script>
{% endblock %}

{% block title %}
    {% if offre is defined %}
        <meta name="viewport" content="initial-scale=1">
        Formulaire de modification de l'offre n° {{ offre.getId_offre() }}
    {% else %}
        <meta name="viewport" content="initial-scale=1">
        Formulaire d'ajout d'offres
    {% endif %}
{% endblock %}

{% block content %}
    <div class="box box-primary">
        <!-- Main content -->
        <div class="box-body">


        {#        FORMULAIRE DE MODIFICATION       D'EXAMEN               #}
        {% if offre is defined %}
        <form class="form-horizontal" method="post" action="{{ path('offre_edited') }}" style="margin-left:20px">

                <div class="form-group">
                    <label for="#">Nom de l'offre<span style="color: red;">*</span></label>
                    {# nom de l'offre#}
                    <input style="border-radius: 3px; width: 50%;" type="text" class="form-control" name="name" placeholder="" value="{{ offre.getId_offre() }}" >
                    <input type="hidden" value="{{ offre.getId_offre() }}" name="id_offre" >
                </div>
                <div class="form-group">
                    <label for="#">Catégorie<span style="color: red;">*</span></label>
                    {#Crée une liste déroulante des nom de classe#}
                    <select class="form-control" name="categorie" style="border-radius: 3px; width: 50%;">
                    
                    </select>
                </div>
                <div class="form-group">
                    <label for="#">Compétences<span style="color: red;">*</span></label>
                    {#Crée une liste déroulante des nom de classe#}
                    <select class="form-control" name="classname" style="border-radius: 3px; width: 50%;">
                   {#     {% for classe in classes %}
                            <option value="{{ classe.getIdClassName() }}">
                                {{ classe.getClassName() }}
                            </option>
                        {% endfor %} #}
                    </select>
                </div>
       
                <div class="form-group">
                    <label for="#">Description</label>
                    {#  description de l'offre#}
                    <input class="form-control" style="width:50%; height:100px; border-radius: 3px" name="description" placeholder="" type="text" value="{{ offre.getDescriptionExamen() }}" >
                </div>
                <div class="form_group">
                    <button type="submit" class="btn btn-info pull-right">Enregistrer</button>
                </div>
            </form>


                {% else %}

                    {#        FORMULAIRE D'AJOUT         D'EXAMEN               #}



             <form class="form-horizontal" method="post" action="{{ path('added_offre') }}" style="margin-left:20px">
            <div class="form-group">
                    <label for="#">Nom de l'offre<span style="color: red;">*</span></label>  
                    {#Ajoute le nom d'un offre#}
                    <input style="border-radius: 3px; width: 50%;" type="text" class="form-control" name="name" placeholder=""/>
                </div>
                  
                <div class="form-group">
                    <label for="#">Catégorie<span style="color: red;">*</span></label>
                    {#Crée une liste déroulante des nom de classe#}
                    <select class="form-control" name="categorie" style="border-radius: 3px; width: 50%;">
                     
                    </select>
                </div>
                <div class="form-group">
                    <label for="#">Projets<span style="color: red;">*</span></label>
                    {#Crée une liste déroulante des nom de classe#}
                    <select class="form-control" name="projet" style="border-radius: 3px; width: 50%;">
            
                    </select>
                </div>
        
                <div class="form-group">
                    <label for="#">Description</label>
                    {#Ajoute une description à l'offre#}
                    <textarea  class="form-control" rows="3" style="width: 50%;; border-radius: 3px" name="description" placeholder=""></textarea>
                </div>
                <div class="form_group">
                    <button type="submit" class="btn btn-info pull-right">Ajouter</button>
                </div>
             </form>
                {% endif %}

            </div>
            {#Affiche un message d'ajout réussi#}
            {% for success in app.session.getFlashBag.get('success') %}
                <div class="alert alert-success fade">
                    {{ success }}
                </div>
            {% endfor %}
        </form>
    </div>
{% endblock %}

